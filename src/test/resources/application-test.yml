spring:
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    enabled: true
    locations: classpath:db/migration

app:
  jwt:
    secret: test-only-secret-not-for-prod-0123456789
    expiration-ms: 3600000

logging:
  level:
    root: warn
    br.com.fiap.restaurant: info
